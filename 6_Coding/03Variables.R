xvar <- 2
yvar = 4 # you can use <- or = for assignments
result <- (xvar + yvar) * yvar
msg <- "The result is "
print(msg)
print(result) # can be printed together if required
xvar <- runif(1,0,100)
xvar